---
phase: 04-internalizacao-do-extrator-torre-de-controle
plan: 02
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - server/routes/google.js
  - server/services/google-oauth.js
  - server/index.js
autonomous: true
requirements:
  - EXT-01

must_haves:
  truths:
    - "POST /api/google/connect-drive/start inicia OAuth com scopes drive.readonly + documents.readonly + presentations.readonly"
    - "GET /api/google/connect-drive/callback troca code por tokens, persiste refresh_token criptografado em google_oauth_tokens, e redireciona para resume_url da sessao quando houver"
    - "GET /api/google/status retorna { connected, email, expires_at, scopes_ok, revoked }"
    - "POST /api/google/disconnect revoga o refresh_token remotamente (oauth2.googleapis.com/revoke) e marca revoked_at"
    - "server/services/google-oauth.js expoe getAccessToken(userId) que cacheia em memoria ate expires_at e refresca via refresh_token"
    - "Rota /api/auth/google (login-only) permanece funcional com scopes openid email profile"
  artifacts:
    - path: "server/routes/google.js"
      provides: "Endpoints OAuth Drive para frontend"
      contains: "/connect-drive/start"
    - path: "server/services/google-oauth.js"
      provides: "Servico de gestao de tokens Google per-user"
      contains: "getAccessToken"
  key_links:
    - from: "server/routes/google.js"
      to: "server/services/google-oauth.js"
      via: "storeTokens, revokeTokens, getStatus"
      pattern: "google-oauth"
    - from: "server/services/google-oauth.js"
      to: "server/lib/crypto.js"
      via: "encrypt(refresh_token) / decrypt(row.refresh_token_enc)"
      pattern: "encrypt"
    - from: "server/index.js"
      to: "server/routes/google.js"
      via: "app.use('/api/google', googleRouter)"
      pattern: "google"
---

<objective>
Adicionar endpoints OAuth Google "Drive/Docs/Slides readonly" per-user sem quebrar o login Google existente (D-03).

Purpose: dar ao frontend o contrato para Block-on-trigger modal (start + callback + status + disconnect) e ao backend um servico que fornece access_token valido (cache em memoria + refresh automatico) para os extratores.
Output: 1 rota nova (`/api/google/*`) + 1 servico de gestao de tokens + registro da rota no `server/index.js`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-CONTEXT.md
@.planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-UI-SPEC.md
@.planning/research/n8n-workflow-auditoria-saber-interno.md

<interfaces>
Existing OAuth login flow (server/routes/auth.js:55-157):
```js
router.get('/google', (req, res) => {
  // scope: 'openid email profile', access_type: 'offline', prompt: 'select_account'
  res.redirect(`https://accounts.google.com/o/oauth2/v2/auth?${params}`)
})

router.get('/google/callback', async (req, res) => {
  // POST https://oauth2.googleapis.com/token with code/client_id/client_secret/redirect_uri/grant_type
  // -> tokens.access_token + tokens.refresh_token (refresh so vem se access_type=offline + primeira consent)
})
```

Existing pool (server/lib/db.js):
```js
import pool from '../lib/db.js'
await pool.query('SELECT ... FROM dashboards_hub.google_oauth_tokens WHERE user_id = $1', [userId])
```

Google revoke endpoint:
```
POST https://oauth2.googleapis.com/revoke?token=REFRESH_TOKEN
Content-Type: application/x-www-form-urlencoded
```

Env vars ja existentes:
- GOOGLE_CLIENT_ID
- GOOGLE_CLIENT_SECRET
- GOOGLE_CALLBACK_URL (usado pelo login; connect-drive usa callback distinto)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar server/services/google-oauth.js</name>
  <files>server/services/google-oauth.js</files>
  <read_first>
    - server/lib/db.js
    - server/lib/crypto.js
    - server/routes/auth.js
    - .planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-CONTEXT.md
  </read_first>
  <action>
Criar `server/services/google-oauth.js` com o servico de gestao de tokens per-user. API publica:

- `storeTokens(userId, { refresh_token, access_token, expires_in, scopes, email })` — upsert em `google_oauth_tokens`, encrypt refresh_token
- `getAccessToken(userId)` — retorna access_token valido (cache em memoria + refresh via refresh_token se expirado); lanca `GoogleReauthRequiredError` se revoked/ausente/grant invalido
- `getStatus(userId)` — `{ connected, email, expires_at, scopes_ok, revoked_at }`
- `revokeTokens(userId)` — chama `oauth2.googleapis.com/revoke` e UPDATE `revoked_at = NOW()`
- Exporta classe `GoogleReauthRequiredError extends Error` com `code = 'google_reauth_required'`

Esqueleto:

```js
// server/services/google-oauth.js
import pool from '../lib/db.js'
import { encrypt, decrypt } from '../lib/crypto.js'

const REQUIRED_SCOPES = ['https://www.googleapis.com/auth/drive.readonly', 'https://www.googleapis.com/auth/documents.readonly', 'https://www.googleapis.com/auth/presentations.readonly']
const CACHE = new Map() // userId -> { access_token, expires_at_ms }

export class GoogleReauthRequiredError extends Error {
  constructor(message, { userId, reason } = {}) {
    super(message)
    this.name = 'GoogleReauthRequiredError'
    this.code = 'google_reauth_required'
    this.userId = userId
    this.reason = reason
  }
}

export async function storeTokens(userId, { refresh_token, access_token, expires_in, scopes, email }) {
  const refresh_token_enc = encrypt(refresh_token)
  const expires_at = new Date(Date.now() + (expires_in || 3600) * 1000)
  await pool.query(`
    INSERT INTO dashboards_hub.google_oauth_tokens (user_id, refresh_token_enc, access_token, expires_at, scopes, connected_at, last_used_at, revoked_at, error_message)
    VALUES ($1, $2, $3, $4, $5, NOW(), NOW(), NULL, NULL)
    ON CONFLICT (user_id) DO UPDATE SET
      refresh_token_enc = EXCLUDED.refresh_token_enc,
      access_token = EXCLUDED.access_token,
      expires_at = EXCLUDED.expires_at,
      scopes = EXCLUDED.scopes,
      connected_at = NOW(),
      last_used_at = NOW(),
      revoked_at = NULL,
      error_message = NULL,
      updated_at = NOW()
  `, [userId, refresh_token_enc, access_token || null, expires_at, scopes || REQUIRED_SCOPES])
  CACHE.set(userId, { access_token, expires_at_ms: expires_at.getTime() })
  // persistir email somente se a coluna existir (cross-fase). Por ora ignora.
}

export async function getAccessToken(userId) {
  const cached = CACHE.get(userId)
  if (cached && cached.expires_at_ms - 60_000 > Date.now()) return cached.access_token

  const { rows } = await pool.query('SELECT refresh_token_enc, revoked_at FROM dashboards_hub.google_oauth_tokens WHERE user_id = $1', [userId])
  const row = rows[0]
  if (!row) throw new GoogleReauthRequiredError('sem token Google para este user', { userId, reason: 'missing' })
  if (row.revoked_at) throw new GoogleReauthRequiredError('token revogado', { userId, reason: 'revoked' })

  const refresh_token = decrypt(row.refresh_token_enc)
  const res = await globalThis.fetch('https://oauth2.googleapis.com/token', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams({
      client_id: process.env.GOOGLE_CLIENT_ID,
      client_secret: process.env.GOOGLE_CLIENT_SECRET,
      refresh_token,
      grant_type: 'refresh_token'
    })
  })
  if (!res.ok) {
    const body = await res.text()
    await pool.query('UPDATE dashboards_hub.google_oauth_tokens SET revoked_at = NOW(), error_message = $2 WHERE user_id = $1', [userId, body.slice(0, 500)])
    CACHE.delete(userId)
    throw new GoogleReauthRequiredError(`refresh falhou: ${res.status}`, { userId, reason: 'invalid_grant' })
  }
  const tokens = await res.json()
  const expires_at = new Date(Date.now() + (tokens.expires_in || 3600) * 1000)
  await pool.query('UPDATE dashboards_hub.google_oauth_tokens SET access_token = $2, expires_at = $3, last_used_at = NOW(), error_message = NULL, updated_at = NOW() WHERE user_id = $1', [userId, tokens.access_token, expires_at])
  CACHE.set(userId, { access_token: tokens.access_token, expires_at_ms: expires_at.getTime() })
  return tokens.access_token
}

export async function getStatus(userId) {
  const { rows } = await pool.query('SELECT scopes, connected_at, last_used_at, revoked_at, expires_at, error_message FROM dashboards_hub.google_oauth_tokens WHERE user_id = $1', [userId])
  const row = rows[0]
  if (!row) return { connected: false }
  const scopes_ok = REQUIRED_SCOPES.every(s => row.scopes.includes(s))
  return {
    connected: !row.revoked_at,
    scopes_ok,
    connected_at: row.connected_at,
    last_used_at: row.last_used_at,
    revoked_at: row.revoked_at,
    expires_at: row.expires_at,
    error_message: row.error_message
  }
}

export async function revokeTokens(userId) {
  const { rows } = await pool.query('SELECT refresh_token_enc FROM dashboards_hub.google_oauth_tokens WHERE user_id = $1', [userId])
  if (rows[0]?.refresh_token_enc) {
    try {
      const refresh_token = decrypt(rows[0].refresh_token_enc)
      await globalThis.fetch(`https://oauth2.googleapis.com/revoke?token=${encodeURIComponent(refresh_token)}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/x-www-form-urlencoded' }
      })
    } catch (err) {
      console.error(`[${new Date().toISOString()}] Google revoke error:`, err.message)
    }
  }
  await pool.query('UPDATE dashboards_hub.google_oauth_tokens SET revoked_at = NOW(), access_token = NULL WHERE user_id = $1', [userId])
  CACHE.delete(userId)
}

export { REQUIRED_SCOPES }
```

Nao introduzir deps externas — usar `globalThis.fetch` e `pg` ja presentes.
  </action>
  <verify>
    <automated>grep -c "getAccessToken\|storeTokens\|revokeTokens\|getStatus\|GoogleReauthRequiredError\|REQUIRED_SCOPES" server/services/google-oauth.js</automated>
  </verify>
  <acceptance_criteria>
    - server/services/google-oauth.js exists
    - File exports `storeTokens`, `getAccessToken`, `getStatus`, `revokeTokens`
    - File exports class `GoogleReauthRequiredError` with `code = 'google_reauth_required'`
    - File imports `encrypt` and `decrypt` from `../lib/crypto.js`
    - File contains the 3 required scopes: `drive.readonly`, `documents.readonly`, `presentations.readonly`
    - File uses `ON CONFLICT (user_id) DO UPDATE` for upsert
    - File uses in-memory Map for access_token cache
    - File calls `https://oauth2.googleapis.com/token` for refresh and `https://oauth2.googleapis.com/revoke` for revoke
  </acceptance_criteria>
  <done>google-oauth.js funcional com cache, refresh automatico e revoke remoto</done>
</task>

<task type="auto">
  <name>Task 2: Criar server/routes/google.js</name>
  <files>server/routes/google.js</files>
  <read_first>
    - server/routes/auth.js
    - server/services/google-oauth.js
    - server/middleware/requireAuth.js
    - .planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-UI-SPEC.md
  </read_first>
  <action>
Criar `server/routes/google.js` com os endpoints OAuth "Drive readonly" per-user e uma rota de status/disconnect, todos protegidos por `requireAuth`.

Endpoints:

1. `POST /connect-drive/start` (body opcional `{ resume_url }`) — salva `req.session.googleOauthResume = resume_url` e retorna `{ authorize_url }` apontando para `accounts.google.com/o/oauth2/v2/auth` com:
   - `client_id`, `redirect_uri` (novo — `GOOGLE_DRIVE_CALLBACK_URL` ou fallback para `${GOOGLE_CALLBACK_URL}/drive`)
   - `scope`: `drive.readonly documents.readonly presentations.readonly`
   - `access_type=offline`, `prompt=consent` (obriga refresh_token)
   - `state=<csrf token em sessao>`

2. `GET /connect-drive/callback?code=...&state=...` — valida `state`, troca code por tokens (POST `oauth2.googleapis.com/token`), chama `storeTokens(req.session.user.id, tokens)`, redireciona para `req.session.googleOauthResume || '/'` e limpa a chave da sessao.

3. `GET /status` — retorna `getStatus(req.session.user.id)`.

4. `POST /disconnect` — chama `revokeTokens(req.session.user.id)` e retorna `{ ok: true }`.

Todas as 4 rotas usam middleware `requireAuth` (ja existe em `server/middleware/requireAuth.js`).

Garantir que o callback persiste a sessao com `req.session.save()` antes de redirecionar (padrao ja usado em auth.js) para evitar race condition.

Se o Google retornar erro (`code` ausente ou `error` query param), redirecionar para `req.session.googleOauthResume || '/'` com query `?google_oauth_error=1`.
  </action>
  <verify>
    <automated>grep -c "/connect-drive/start\|/connect-drive/callback\|/status\|/disconnect\|requireAuth\|accounts.google.com/o/oauth2" server/routes/google.js</automated>
  </verify>
  <acceptance_criteria>
    - server/routes/google.js exists
    - File imports `requireAuth` from `../middleware/requireAuth.js`
    - File defines POST `/connect-drive/start` with session state + authorize_url response
    - File defines GET `/connect-drive/callback` that calls `storeTokens`
    - File defines GET `/status` that returns `getStatus(req.session.user.id)`
    - File defines POST `/disconnect` that calls `revokeTokens`
    - File's authorize_url includes scopes `drive.readonly`, `documents.readonly`, `presentations.readonly`
    - File's authorize_url includes `access_type=offline` and `prompt=consent`
    - File calls `req.session.save(` before redirect in callback
  </acceptance_criteria>
  <done>server/routes/google.js com 4 endpoints funcionais (start, callback, status, disconnect)</done>
</task>

<task type="auto">
  <name>Task 3: Registrar router em server/index.js</name>
  <files>server/index.js</files>
  <read_first>
    - server/index.js
  </read_first>
  <action>
Em `server/index.js`:

1. Adicionar import do novo router (perto dos outros routers):
```js
import googleRouter from './routes/google.js'
```

2. Registrar a rota (antes do middleware 404 e apos as rotas `/api/auth`):
```js
app.use('/api/google', googleRouter)
```

NAO alterar configuracao de sessao. NAO alterar rotas existentes.
  </action>
  <verify>
    <automated>grep -c "googleRouter\|'/api/google'" server/index.js</automated>
  </verify>
  <acceptance_criteria>
    - server/index.js contains `import googleRouter from './routes/google.js'`
    - server/index.js contains `app.use('/api/google', googleRouter)`
    - Existing routes (auth, api, admin) remain registered
  </acceptance_criteria>
  <done>Router /api/google registrado no app Express</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Backend | Frontend envia `resume_url` — backend NUNCA deve confiar e redirecionar sem validar (open redirect) |
| Backend ↔ Google | POST `oauth2.googleapis.com/token` transporta `client_secret` e `refresh_token` (TLS obrigatorio) |
| Session cookie | `googleOauthResume` e `state` ficam no session store Postgres; httpOnly + secure em producao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | S (Spoofing) | callback `?state=` | mitigate | Validar `state` contra `req.session.googleOauthState` (CSRF). Gerar com `randomBytes(16).toString('hex')` no /start. Rejeitar se mismatch. |
| T-04-02-02 | I (Information disclosure) | `GET /status` | mitigate | `requireAuth` garante filtro por `req.session.user.id`. Jamais vazar `refresh_token_enc` no response. Apenas email/timestamps. |
| T-04-02-03 | T (Tampering) | `resume_url` | mitigate | Validar que `resume_url` comeca com `/` e nao contem `://` antes de redirecionar (previne open redirect). Fallback `/` se invalido. |
| T-04-02-04 | R (Repudiation) | revoke | accept | `revoked_at` timestamp. Sem audit log separado — suficiente para o escopo. |
| T-04-02-05 | E (Elevation of privilege) | connect-drive/callback | mitigate | `requireAuth` garante que `req.session.user.id` e sempre do user logado; impossivel conectar token de user A com sessao de user B. |

</threat_model>

<verification>
- `npm run build` completa sem erros
- `npm run dev` inicia e `curl -i http://localhost:3000/api/google/status` retorna 401 (sem sessao)
- Login manual, depois `POST /api/google/connect-drive/start` devolve `authorize_url` valido (regex `^https://accounts.google.com/o/oauth2/v2/auth\?`)
- grep confirma registro do router em server/index.js
</verification>

<success_criteria>
- `GET /api/google/status` responde 200 com `{ connected: false }` para user sem token
- `POST /api/google/connect-drive/start` responde 200 com `authorize_url` contendo os 3 scopes e `prompt=consent`
- `GET /api/google/connect-drive/callback` armazena refresh_token encrypted + access_token + expires_at, e redireciona para `resume_url`
- `POST /api/google/disconnect` revoga remotamente (melhor esforco) e marca `revoked_at`
- `getAccessToken(userId)` chama refresh automaticamente quando `expires_at` proximo e lanca `GoogleReauthRequiredError` em caso de invalid_grant
</success_criteria>

<output>
After completion, create `.planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-02-SUMMARY.md`
</output>
