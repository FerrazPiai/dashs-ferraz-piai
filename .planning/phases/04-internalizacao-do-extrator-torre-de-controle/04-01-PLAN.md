---
phase: 04-internalizacao-do-extrator-torre-de-controle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/crypto.js
  - migrations/016_google_oauth_tokens.sql
  - .planning/REQUIREMENTS.md
  - .env.example
autonomous: true
requirements:
  - EXT-01

must_haves:
  truths:
    - "server/lib/crypto.js exporta encrypt(plaintext) e decrypt(cipherB64) usando AES-256-GCM"
    - "Migration 016_google_oauth_tokens.sql cria tabela dashboards_hub.google_oauth_tokens com user_id FK, refresh_token_enc, access_token, expires_at, scopes[], connected_at, last_used_at, revoked_at, error_message"
    - "tc_jobs ganha coluna triggered_by_user_id nullable (para D-01/D-02 fallback)"
    - "REQUIREMENTS.md lista EXT-01..EXT-06 com texto canonico aprovado"
    - ".env.example lista TOKEN_ENC_KEY, INTERNAL_EXTRACTORS, FIGMA_TOKEN, MIRO_TOKEN"
  artifacts:
    - path: "server/lib/crypto.js"
      provides: "Utilitario AES-256-GCM para refresh tokens"
      contains: "createCipheriv"
    - path: "migrations/016_google_oauth_tokens.sql"
      provides: "Schema SQL novo + alter de tc_jobs"
      contains: "CREATE TABLE"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requisitos EXT-01..EXT-06 registrados"
      contains: "EXT-06"
  key_links:
    - from: "server/lib/crypto.js"
      to: "process.env.TOKEN_ENC_KEY"
      via: "Buffer.from(key, 'base64')"
      pattern: "TOKEN_ENC_KEY"
---

<objective>
Estabelecer as fundacoes da internalizacao: utilitario de criptografia AES-256-GCM para refresh tokens, migration SQL que cria `dashboards_hub.google_oauth_tokens` e adiciona `triggered_by_user_id` em `tc_jobs`, registro canonico dos requisitos EXT-01..EXT-06 em `REQUIREMENTS.md` e atualizacao do `.env.example` com as variaveis novas.

Purpose: sem esse plano, nada depois e possivel — nenhum extrator interno conecta ao Google, nenhum job carrega user_id, nenhum checker consegue mapear plan→requirement.
Output: 1 arquivo de util + 1 migration SQL + REQUIREMENTS.md atualizado + .env.example atualizado.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-CONTEXT.md
@.planning/notes/decisao-internalizacao-extracao-tc.md

<interfaces>
Pattern Postgres migration (from migrations/015_tc_alert_system.sql):
```sql
SET search_path TO dashboards_hub;

CREATE TABLE IF NOT EXISTS <name> (
  id BIGSERIAL PRIMARY KEY,
  ...
);
```

Pattern ESM export no-default (from server/lib/db.js):
```js
import pg from 'pg'
const { Pool } = pg
const pool = new Pool({ ... })
export default pool
```

node:crypto AES-256-GCM (reference):
```js
import { createCipheriv, createDecipheriv, randomBytes } from 'node:crypto'
// cipher.update(plaintext, 'utf8', 'base64') + cipher.final('base64')
// iv (12 bytes), tag (16 bytes), ciphertext -> all concatenated and base64'd
```

.env.example current tail:
```
N8N_EXTRACT_WEBHOOK_URL=https://.../webhook/...
MISTRAL_API_KEY=...
OPENAI_API_KEY=...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar server/lib/crypto.js (AES-256-GCM)</name>
  <files>server/lib/crypto.js</files>
  <read_first>
    - server/lib/db.js
    - .planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-CONTEXT.md
  </read_first>
  <action>
Criar `server/lib/crypto.js` com utilitario AES-256-GCM usando `node:crypto` nativo.

Conteudo exato:

```js
// server/lib/crypto.js
// AES-256-GCM para refresh tokens Google (D-01). Chave em TOKEN_ENC_KEY (32 bytes base64).
// Formato do cipher: base64(iv || tag || ciphertext) — iv=12B, tag=16B.
import { createCipheriv, createDecipheriv, randomBytes } from 'node:crypto'

const IV_LEN = 12
const TAG_LEN = 16

function getKey() {
  const raw = process.env.TOKEN_ENC_KEY
  if (!raw) throw new Error('TOKEN_ENC_KEY nao configurado')
  const key = Buffer.from(raw, 'base64')
  if (key.length !== 32) {
    throw new Error(`TOKEN_ENC_KEY deve ter 32 bytes em base64, recebido ${key.length}`)
  }
  return key
}

export function encrypt(plaintext) {
  if (plaintext == null) throw new Error('encrypt: plaintext obrigatorio')
  const key = getKey()
  const iv = randomBytes(IV_LEN)
  const cipher = createCipheriv('aes-256-gcm', key, iv)
  const ciphertext = Buffer.concat([cipher.update(String(plaintext), 'utf8'), cipher.final()])
  const tag = cipher.getAuthTag()
  return Buffer.concat([iv, tag, ciphertext]).toString('base64')
}

export function decrypt(cipherB64) {
  if (!cipherB64) throw new Error('decrypt: cipherB64 obrigatorio')
  const key = getKey()
  const buf = Buffer.from(cipherB64, 'base64')
  if (buf.length < IV_LEN + TAG_LEN + 1) throw new Error('cipher invalido (tamanho curto)')
  const iv = buf.subarray(0, IV_LEN)
  const tag = buf.subarray(IV_LEN, IV_LEN + TAG_LEN)
  const ciphertext = buf.subarray(IV_LEN + TAG_LEN)
  const decipher = createDecipheriv('aes-256-gcm', key, iv)
  decipher.setAuthTag(tag)
  return Buffer.concat([decipher.update(ciphertext), decipher.final()]).toString('utf8')
}

export function generateKey() {
  // Helper para operadores gerarem TOKEN_ENC_KEY: `node -e "import('./server/lib/crypto.js').then(m=>console.log(m.generateKey()))"`
  return randomBytes(32).toString('base64')
}
```

NAO introduzir deps externas. NAO armazenar a chave em DB. Falha com erro tipado se chave ausente/invalida na primeira chamada.
  </action>
  <verify>
    <automated>grep -c "aes-256-gcm\|createCipheriv\|createDecipheriv\|TOKEN_ENC_KEY" server/lib/crypto.js</automated>
  </verify>
  <acceptance_criteria>
    - server/lib/crypto.js exists
    - File contains `export function encrypt(`
    - File contains `export function decrypt(`
    - File contains `export function generateKey(`
    - File uses `aes-256-gcm` literal string
    - File imports from `node:crypto`
    - File reads `process.env.TOKEN_ENC_KEY` and base64-decodes it to 32 bytes
    - File uses IV of 12 bytes and GCM tag of 16 bytes
  </acceptance_criteria>
  <done>crypto.js com encrypt/decrypt AES-256-GCM funcional usando TOKEN_ENC_KEY</done>
</task>

<task type="auto">
  <name>Task 2: Migration 016_google_oauth_tokens.sql</name>
  <files>migrations/016_google_oauth_tokens.sql</files>
  <read_first>
    - migrations/001_create_hub_tables.sql
    - migrations/015_tc_alert_system.sql
    - .planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-CONTEXT.md
  </read_first>
  <action>
Criar `migrations/016_google_oauth_tokens.sql` com:

1. Tabela `google_oauth_tokens` no schema `dashboards_hub` (uma linha por user Google conectado).
2. ALTER na tabela `tc_jobs` (se existir) adicionando `triggered_by_user_id BIGINT NULL`.

Conteudo exato:

```sql
-- 016: tabela de tokens OAuth Google per-user (D-01) + triggered_by_user_id em tc_jobs (D-01/D-02)
SET search_path TO dashboards_hub;

CREATE TABLE IF NOT EXISTS google_oauth_tokens (
  id BIGSERIAL PRIMARY KEY,
  user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  refresh_token_enc TEXT NOT NULL,
  access_token TEXT NULL,
  expires_at TIMESTAMPTZ NULL,
  scopes TEXT[] NOT NULL DEFAULT '{}',
  connected_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  last_used_at TIMESTAMPTZ NULL,
  revoked_at TIMESTAMPTZ NULL,
  error_message TEXT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE (user_id)
);

CREATE INDEX IF NOT EXISTS idx_google_oauth_tokens_revoked
  ON google_oauth_tokens(revoked_at)
  WHERE revoked_at IS NULL;

-- tc_jobs.triggered_by_user_id: nullable pois jobs legados e scheduled jobs (futuro D-02) podem nao ter
DO $$
BEGIN
  IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema='dashboards_hub' AND table_name='tc_jobs') THEN
    ALTER TABLE tc_jobs
      ADD COLUMN IF NOT EXISTS triggered_by_user_id BIGINT NULL REFERENCES users(id) ON DELETE SET NULL;
    CREATE INDEX IF NOT EXISTS idx_tc_jobs_triggered_by_user_id ON tc_jobs(triggered_by_user_id);
  END IF;
END$$;
```

NAO remover/alterar colunas existentes. NAO rodar a migration aqui — apenas criar o arquivo.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS google_oauth_tokens\|triggered_by_user_id\|refresh_token_enc" migrations/016_google_oauth_tokens.sql</automated>
  </verify>
  <acceptance_criteria>
    - migrations/016_google_oauth_tokens.sql exists
    - File contains `SET search_path TO dashboards_hub`
    - File contains `CREATE TABLE IF NOT EXISTS google_oauth_tokens`
    - File defines columns: user_id, refresh_token_enc, access_token, expires_at, scopes, connected_at, last_used_at, revoked_at, error_message
    - File has `UNIQUE (user_id)` constraint
    - File has `FOREIGN KEY` / `REFERENCES users(id)` with `ON DELETE CASCADE`
    - File has `ALTER TABLE tc_jobs ADD COLUMN IF NOT EXISTS triggered_by_user_id`
    - File uses `DO $$ ... END$$` block for conditional tc_jobs alter
  </acceptance_criteria>
  <done>Migration 016 cria google_oauth_tokens + adiciona triggered_by_user_id em tc_jobs</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar EXT-01..EXT-06 ao REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
Append uma nova secao `## Extrator Torre de Controle (EXT)` ao final de `.planning/REQUIREMENTS.md` com os 6 requisitos canonicos:

```markdown

## Extrator Torre de Controle (EXT)

Requisitos da fase 4 — internalizacao do workflow n8n `uiUUXegcBHe3z2fg`.
Decisao arquitetural em `.planning/notes/decisao-internalizacao-extracao-tc.md`.

### EXT-01 OAuth Google per-user
Gestor conecta sua conta Google uma unica vez (scopes `drive.readonly` + `documents.readonly` + `presentations.readonly`). O `refresh_token` fica armazenado criptografado (AES-256-GCM) na tabela `dashboards_hub.google_oauth_tokens`. Reauth via banner+retry quando o token expira/e revogado. Ownership do job segue trigger-owner (`triggered_by_user_id`) com fallback Kommo-resp (`users.kommo_user_id` ↔ `tc_kommo_users.responsible_user_id`).

### EXT-02 Google Slides via API nativa
Extracao de Slides usa `slides.presentations.get` para estrutura (text boxes, tabelas, speaker notes, image IDs) + GPT-4o vision com `detail=high` para cada imagem embutida. Sem Mistral OCR. Sem download de PDF. Prompts OpenAI portados literal do workflow n8n atual (secao 6 de `.planning/research/n8n-workflow-auditoria-saber-interno.md`).

### EXT-03 Google Docs via API nativa
Extracao de transcricoes em Google Docs usa `docs.documents.get` e concatena `body.content[]` (paragraphs, textRuns, tables, headings) preservando ordem. Sem Mistral OCR. Sem download de PDF.

### EXT-04 Figma interno
Extracao Figma roda 100% no backend via Figma REST API com `FIGMA_TOKEN` centralizado no `.env`. Prompts do branch figma portados literal. Preserva `auditoria_narrativa_integral` se paridade exigir.

### EXT-05 Miro interno com paginacao
Extracao Miro roda 100% no backend via Miro REST API v2 com `MIRO_TOKEN` centralizado no `.env`. `/v2/boards/{id}/items` e paginado via cursor (`cursor` + `limit=50`) ate `cursor` ausente — corrige o bug atual do workflow n8n que so le pagina 1 e trunca boards grandes.

### EXT-06 Cutover n8n
`extractViaN8n`, `N8N_EXTRACT_WEBHOOK_URL`, `PLATFORM_TO_N8N` e `n8nLimiter` removidos do codigo. Feature flag `INTERNAL_EXTRACTORS=transcricao,google,figma,miro` em `.env` permite rollout por plataforma durante cutover. Workflow n8n `uiUUXegcBHe3z2fg` e ARQUIVADO (nao deletado) como referencia historica. `.env.example` e `.planning/codebase/INTEGRATIONS.md` atualizados.
```

Se a secao ja existir (check com grep antes), NAO duplicar — apenas confirmar conteudo.
  </action>
  <verify>
    <automated>grep -c "^### EXT-0[1-6]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains line matching `^## Extrator Torre de Controle`
    - .planning/REQUIREMENTS.md contains line matching `^### EXT-01 OAuth Google per-user`
    - .planning/REQUIREMENTS.md contains line matching `^### EXT-02 Google Slides via API nativa`
    - .planning/REQUIREMENTS.md contains line matching `^### EXT-03 Google Docs via API nativa`
    - .planning/REQUIREMENTS.md contains line matching `^### EXT-04 Figma interno`
    - .planning/REQUIREMENTS.md contains line matching `^### EXT-05 Miro interno com paginacao`
    - .planning/REQUIREMENTS.md contains line matching `^### EXT-06 Cutover n8n`
    - Existing requirements sections remain unchanged
  </acceptance_criteria>
  <done>REQUIREMENTS.md tem secao EXT com 6 requisitos canonicos</done>
</task>

<task type="auto">
  <name>Task 4: Atualizar .env.example</name>
  <files>.env.example</files>
  <read_first>
    - .env.example
  </read_first>
  <action>
Append ao final de `.env.example` (apos a ultima linha existente) um bloco com as variaveis novas:

```
# ── Extrator Torre de Controle (fase 4) ──
# Gerar com: node -e "import('./server/lib/crypto.js').then(m=>console.log(m.generateKey()))"
TOKEN_ENC_KEY=
# Lista de plataformas que usam extrator interno. As nao listadas caem para o extrator n8n ate o cutover final.
# Durante cutover: transcricao,google,figma,miro (tudo). Vazio = continuar usando n8n em todas.
INTERNAL_EXTRACTORS=
# Tokens centralizados Figma e Miro (nao confundir com OAuth per-user do Google)
FIGMA_TOKEN=
MIRO_TOKEN=
```

NAO remover nem alterar variaveis existentes. NAO adicionar valores reais.
  </action>
  <verify>
    <automated>grep -c "TOKEN_ENC_KEY\|INTERNAL_EXTRACTORS\|FIGMA_TOKEN\|MIRO_TOKEN" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains `TOKEN_ENC_KEY=`
    - .env.example contains `INTERNAL_EXTRACTORS=`
    - .env.example contains `FIGMA_TOKEN=`
    - .env.example contains `MIRO_TOKEN=`
    - .env.example still contains pre-existing `SESSION_SECRET` and `DATABASE_URL` entries
  </acceptance_criteria>
  <done>.env.example lista TOKEN_ENC_KEY, INTERNAL_EXTRACTORS, FIGMA_TOKEN, MIRO_TOKEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process ↔ Env | `TOKEN_ENC_KEY` vem do ambiente; leakage no processo exporia todos os refresh_tokens ao ser combinado com dump do DB |
| Process ↔ Postgres | `refresh_token_enc` trafega em cleartext entre Node e Postgres (via TLS do pool); nunca em log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | I (Information disclosure) | server/lib/crypto.js | mitigate | Chave jamais escrita em log; `getKey()` lanca erro se ausente; ciphertext sempre contem IV aleatorio (nao reuse) |
| T-04-01-02 | T (Tampering) | google_oauth_tokens | mitigate | GCM authTag detecta adulteracao do ciphertext; `decrypt()` lanca se tag invalida |
| T-04-01-03 | E (Elevation of privilege) | tc_jobs.triggered_by_user_id | accept | FK com ON DELETE SET NULL — se user for deletado, job perde ownership mas fica auditavel. Impacto aceito: job cai no fallback D-02 ou falha com `google_reauth_required` |
| T-04-01-04 | R (Repudiation) | google_oauth_tokens | accept | `connected_at`/`last_used_at`/`revoked_at` timestamps fornecem rastro basico. Logs auditoria completos ficam para fase futura se virar compliance. |

</threat_model>

<verification>
- `npm run build` completa sem erros (crypto.js e ESM valido)
- grep confirma nomes das funcoes e constantes em crypto.js
- grep confirma nome da tabela e colunas na migration
- grep confirma secao EXT em REQUIREMENTS.md e variaveis em .env.example
- Manual: rodar a migration no Postgres local e `SELECT * FROM dashboards_hub.google_oauth_tokens LIMIT 1` nao da erro
</verification>

<success_criteria>
- server/lib/crypto.js funcional (AES-256-GCM, chave em TOKEN_ENC_KEY)
- migrations/016_google_oauth_tokens.sql existe e e idempotente (IF NOT EXISTS)
- REQUIREMENTS.md tem EXT-01..EXT-06 canonicos
- .env.example lista TOKEN_ENC_KEY, INTERNAL_EXTRACTORS, FIGMA_TOKEN, MIRO_TOKEN
</success_criteria>

<output>
After completion, create `.planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-01-SUMMARY.md`
</output>
