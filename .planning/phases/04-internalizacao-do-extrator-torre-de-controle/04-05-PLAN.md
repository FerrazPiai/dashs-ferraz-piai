---
phase: 04-internalizacao-do-extrator-torre-de-controle
plan: 05
type: execute
wave: 3
depends_on:
  - "04-03"
  - "04-04"
files_modified:
  - server/services/extractors/index.js
  - server/services/tc-analyzer.js
  - server/services/tc-job-worker.js
autonomous: true
requirements:
  - EXT-01
  - EXT-06

must_haves:
  truths:
    - "server/services/extractors/index.js expoe dispatchExtractor(platform, url, { userId }) que roteia para o extrator interno quando a plataforma esta em INTERNAL_EXTRACTORS, ou chama extractViaN8n legado caso contrario"
    - "tc-analyzer.js runAnalysis aceita userId e propaga ate dispatchExtractor"
    - "tc-analyzer.js captura GoogleReauthRequiredError e persiste status_avaliacao=incompleta com code=google_reauth_required"
    - "tc-job-worker.js propaga triggered_by_user_id do job payload para runAnalysis; se null, resolve via fallback D-02 (users.kommo_user_id ↔ tc_kommo_users.responsible_user_id)"
  artifacts:
    - path: "server/services/extractors/index.js"
      provides: "Dispatcher central com feature flag"
      contains: "dispatchExtractor"
    - path: "server/services/tc-analyzer.js"
      provides: "Integracao com dispatcher + fail-mark reauth"
      contains: "dispatchExtractor"
    - path: "server/services/tc-job-worker.js"
      provides: "Propagacao de user_id + fallback Kommo"
      contains: "triggered_by_user_id"
  key_links:
    - from: "server/services/tc-analyzer.js"
      to: "server/services/extractors/index.js"
      via: "dispatchExtractor(platform, url, { userId })"
      pattern: "dispatchExtractor"
    - from: "server/services/tc-job-worker.js"
      to: "server/services/tc-analyzer.js"
      via: "runAnalysis({ ..., userId })"
      pattern: "runAnalysis"
---

<objective>
Conectar os 4 extratores internos ao pipeline existente via dispatcher com feature flag `INTERNAL_EXTRACTORS`, propagar `user_id` do trigger-owner em todo o fluxo (D-01) com fallback Kommo (D-02), e implementar fail-mark mid-job (D-04) quando o token Google expira.

Purpose: habilitar o cutover gradual por plataforma sem quebrar o fluxo atual; dar ao frontend um erro tipado para ativar o banner de reauth.
Output: 1 arquivo novo (dispatcher) + 2 arquivos alterados (tc-analyzer, tc-job-worker).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-CONTEXT.md
@.planning/research/n8n-workflow-auditoria-saber-interno.md

<interfaces>
Existing tc-analyzer.js:
- `N8N_URL = process.env.N8N_EXTRACT_WEBHOOK_URL`
- `n8nLimiter = createRateLimiter({...})`
- `PLATFORM_TO_N8N = { slides, reuniao, transcricao, figma, miro }`
- `async function extractViaN8n(url, platform)` — a funcao legada

Existing tc-job-worker.js:
- Consome fila em `dashboards_hub.tc_jobs`
- Chama `runAnalysis({ projetoFaseId, ..., }})` sem userId hoje

Kommo mapping (torre-controle.js:196-200, 668-712):
- `dashboards_hub.tc_kommo_users` tem `id` (Kommo id) + `responsible_user_id` mapping
- `dashboards_hub.users.kommo_user_id` referencia `tc_kommo_users.id`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar server/services/extractors/index.js (dispatcher)</name>
  <files>server/services/extractors/index.js</files>
  <read_first>
    - server/services/extractors/google-docs.js
    - server/services/extractors/google-slides.js
    - server/services/extractors/figma.js
    - server/services/extractors/miro.js
    - server/services/tc-analyzer.js
    - .planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-CONTEXT.md
  </read_first>
  <action>
Criar dispatcher com feature flag `INTERNAL_EXTRACTORS` (csv de plataformas). Rota:

- `platform=transcricao` → `extractGoogleDoc`
- `platform=slides` ou `platform=reuniao` → `extractGoogleSlides`
- `platform=figma` → `extractFigma`
- `platform=miro` → `extractMiro`
- Qualquer outra → cai para `extractViaN8n` legado (importado de tc-analyzer via export)

```js
// server/services/extractors/index.js
import extractGoogleDoc from './google-docs.js'
import extractGoogleSlides from './google-slides.js'
import extractFigma from './figma.js'
import extractMiro from './miro.js'

const INTERNAL_PLATFORM_ROUTES = {
  transcricao: { key: 'transcricao', fn: extractGoogleDoc, requiresUser: true },
  slides:      { key: 'google',      fn: extractGoogleSlides, requiresUser: true },
  reuniao:     { key: 'google',      fn: extractGoogleSlides, requiresUser: true },
  figma:       { key: 'figma',       fn: extractFigma, requiresUser: false },
  miro:        { key: 'miro',        fn: extractMiro, requiresUser: false }
}

export function getEnabledPlatforms() {
  return (process.env.INTERNAL_EXTRACTORS || '')
    .split(',').map(s => s.trim()).filter(Boolean)
}

export function isPlatformInternal(platform) {
  const route = INTERNAL_PLATFORM_ROUTES[platform]
  if (!route) return false
  const enabled = getEnabledPlatforms()
  return enabled.includes(route.key) || enabled.includes('all')
}

export async function dispatchExtractor(platform, url, { userId, fallbackN8n } = {}) {
  const route = INTERNAL_PLATFORM_ROUTES[platform]
  if (route && isPlatformInternal(platform)) {
    if (route.requiresUser && !userId) {
      throw new Error(`dispatchExtractor: platform "${platform}" interno requer userId`)
    }
    return route.fn(url, { userId })
  }
  if (!fallbackN8n) throw new Error(`dispatchExtractor: sem rota para "${platform}" e fallbackN8n ausente`)
  return fallbackN8n(url, platform)
}
```

O `fallbackN8n` e injetado por `tc-analyzer.js` ao chamar — evita dependencia circular.
  </action>
  <verify>
    <automated>grep -c "dispatchExtractor\|INTERNAL_EXTRACTORS\|getEnabledPlatforms\|isPlatformInternal\|fallbackN8n" server/services/extractors/index.js</automated>
  </verify>
  <acceptance_criteria>
    - server/services/extractors/index.js exists
    - File exports `dispatchExtractor`, `isPlatformInternal`, `getEnabledPlatforms`
    - File imports all 4 extractors (google-docs, google-slides, figma, miro)
    - File reads `process.env.INTERNAL_EXTRACTORS`
    - File maps `transcricao -> google-docs`, `slides/reuniao -> google-slides`, `figma -> figma`, `miro -> miro`
    - File supports `all` sentinel in INTERNAL_EXTRACTORS
    - File throws if internal route requires userId and userId is missing
  </acceptance_criteria>
  <done>Dispatcher com feature flag e fallback n8n pronto</done>
</task>

<task type="auto">
  <name>Task 2: Integrar dispatcher em tc-analyzer.js + fail-mark</name>
  <files>server/services/tc-analyzer.js</files>
  <read_first>
    - server/services/tc-analyzer.js
    - server/services/extractors/index.js
    - server/services/google-oauth.js
  </read_first>
  <action>
Edicoes em `server/services/tc-analyzer.js`:

1. Imports (no topo):
```js
import { dispatchExtractor, isPlatformInternal } from './extractors/index.js'
import { GoogleReauthRequiredError } from './google-oauth.js'
```

2. Em `getOrExtract` (ou onde `extractViaN8n(url, plataforma)` e chamada hoje — linha ~79):

Substituir:
```js
const extracted = await extractViaN8n(url, plataforma)
```

Por:
```js
const extracted = await dispatchExtractor(plataforma, url, {
  userId: opts?.userId || null,
  fallbackN8n: extractViaN8n
})
```

(propagar `userId` atraves de toda a cadeia — `runAnalysis` → `getOrExtract` → `dispatchExtractor`).

3. Em `runAnalysis`:
  - Aceitar `userId` no payload (first-class parameter).
  - Passar para `getOrExtract`.
  - Try/catch em volta do loop de plataformas: quando `GoogleReauthRequiredError` e lancado, capturar, persistir no DB:

```js
try {
  // ... loop de plataformas
} catch (err) {
  if (err instanceof GoogleReauthRequiredError) {
    await pool.query(
      `UPDATE dashboards_hub.tc_analises
         SET status_avaliacao = 'incompleta',
             erro_code = 'google_reauth_required',
             erro_mensagem = $2,
             updated_at = NOW()
       WHERE id = $1`,
      [analiseId, err.message]
    )
    // emite log estruturado
    console.error(`[${new Date().toISOString()}] Analise ${analiseId} marcada incompleta: google_reauth_required (user ${err.userId})`)
    return { status: 'incompleta', code: 'google_reauth_required', analiseId }
  }
  throw err
}
```

(Ajustar nomes das tabelas/colunas pelo que ja existe em `tc-analyzer.js`; se `tc_analises.erro_code` nao existir, usar a coluna que ja persiste motivo de incompleta — conferir schema atual em `runAnalysis`.)

4. NAO remover `extractViaN8n` / `N8N_URL` / `n8nLimiter` / `PLATFORM_TO_N8N` AINDA. O cutover final acontece no plano 07.
  </action>
  <verify>
    <automated>grep -c "dispatchExtractor\|GoogleReauthRequiredError\|google_reauth_required\|fallbackN8n" server/services/tc-analyzer.js</automated>
  </verify>
  <acceptance_criteria>
    - server/services/tc-analyzer.js imports `dispatchExtractor` from `./extractors/index.js`
    - server/services/tc-analyzer.js imports `GoogleReauthRequiredError` from `./google-oauth.js`
    - File no longer calls `extractViaN8n` directly in getOrExtract — it calls `dispatchExtractor(..., { fallbackN8n: extractViaN8n })`
    - `runAnalysis` accepts and propagates `userId`
    - File contains `google_reauth_required` literal string
    - File persists `status_avaliacao = 'incompleta'` when GoogleReauthRequiredError is caught
    - `extractViaN8n` function definition remains (will be removed in plan 07)
  </acceptance_criteria>
  <done>tc-analyzer.js usa dispatchExtractor e captura GoogleReauthRequiredError com fail-mark</done>
</task>

<task type="auto">
  <name>Task 3: Propagar user_id em tc-job-worker.js + fallback Kommo</name>
  <files>server/services/tc-job-worker.js</files>
  <read_first>
    - server/services/tc-job-worker.js
    - server/routes/torre-controle.js
    - server/lib/db.js
  </read_first>
  <action>
Em `server/services/tc-job-worker.js`:

1. Ao ler um job da fila, extrair `triggered_by_user_id` do row (a coluna foi adicionada pela migration 016).

2. Se `triggered_by_user_id` null, resolver via fallback Kommo (D-02) em uma query:
```sql
SELECT u.id AS user_id
  FROM dashboards_hub.tc_kommo_users k
  JOIN dashboards_hub.users u ON u.kommo_user_id = k.id
 WHERE k.responsible_user_id = (
    SELECT responsible_user_id FROM dashboards_hub.tc_leads WHERE id = $1
 )
 LIMIT 1
```
(Ajustar o SELECT de `tc_leads` pela coluna real que armazena `responsible_user_id` — pode ser `kommo_responsible_user_id` ou similar; confirmar em torre-controle.js:196-200).

3. Passar `userId` resolvido para `runAnalysis`.

4. Se nem trigger-owner nem fallback Kommo resolverem user_id, persistir job como `failed` com mensagem `sem user_id para autenticar Google` e NAO chamar `runAnalysis`.

5. Log estruturado `[TC-WORKER] job ${id} ownership=${source}` onde `source` e `trigger_owner` ou `kommo_fallback` ou `missing`.

NAO alterar outros comportamentos do worker.
  </action>
  <verify>
    <automated>grep -c "triggered_by_user_id\|kommo_user_id\|TC-WORKER\|userId" server/services/tc-job-worker.js</automated>
  </verify>
  <acceptance_criteria>
    - server/services/tc-job-worker.js reads `triggered_by_user_id` from job row
    - File has fallback query joining `tc_kommo_users` and `users` on `kommo_user_id`
    - File passes resolved `userId` to `runAnalysis`
    - File marks job as `failed` if no user_id resolved (checks both trigger + fallback)
    - Log line contains ownership source (`trigger_owner`/`kommo_fallback`/`missing`)
  </acceptance_criteria>
  <done>Worker propaga user_id com fallback Kommo-resp funcional</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job queue ↔ Worker | Worker confia em `triggered_by_user_id` do row; campo so e setado pelo backend autenticado (rota POST que cria o job) |
| Worker ↔ Google APIs | Cada chamada usa access_token do user resolvido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | E (Elevation of privilege) | fallback Kommo | mitigate | Query so resolve user se o mapping existir em tc_kommo_users + users.kommo_user_id. Se lead nao tem responsavel Kommo, job falha limpo. |
| T-04-05-02 | I (Information disclosure) | tc_analises.erro_mensagem | accept | Guarda mensagem de erro tecnica — aceitavel; nao contem secret (GoogleReauthRequiredError.message e generico) |
| T-04-05-03 | T (Tampering) | INTERNAL_EXTRACTORS env | accept | Config somente-leitura em runtime; mudar exige restart do container |

</threat_model>

<verification>
- `npm run build` passa
- Unit: chamar `dispatchExtractor('figma', 'https://...', { fallbackN8n: fake })` com `INTERNAL_EXTRACTORS=` retorna via fallback
- Unit: mesmo call com `INTERNAL_EXTRACTORS=figma` chama extrator interno
- Manual: simular token revogado → analise persiste com `status_avaliacao=incompleta` + `erro_code=google_reauth_required`
</verification>

<success_criteria>
- Feature flag `INTERNAL_EXTRACTORS` funciona: rota interno vs n8n por plataforma
- `runAnalysis` propaga `userId` e captura `GoogleReauthRequiredError`
- `tc-job-worker.js` resolve ownership via trigger → fallback Kommo → falha limpa
</success_criteria>

<output>
After completion, create `.planning/phases/04-internalizacao-do-extrator-torre-de-controle/04-05-SUMMARY.md`
</output>
