---
phase: 01-polish-visual
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - client/styles/components.css
  - client/dashboards/Admin/index.vue
  - client/dashboards/NpsSatisfacao/index.vue
  - client/dashboards/TxConvSaberMonetizacao/index.vue
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-05

must_haves:
  truths:
    - "components.css tem regras globais de hover/focus e responsividade de scorecards/tabelas"
    - "Admin dashboard usa CSS variables no lugar de cores hardcoded"
    - "NpsSatisfacao usa VLoadingState no lugar de skeleton table"
    - "TxConvSaberMonetizacao usa VLoadingState e VEmptyState no lugar de custom loading/empty states"
    - "Todos os 3 dashboards tem media queries para 1024px e 768px"
  artifacts:
    - path: "client/styles/components.css"
      provides: "Estilos globais de hover, focus-visible e responsividade expandida"
      contains: "focus-visible"
    - path: "client/dashboards/Admin/index.vue"
      provides: "Admin dashboard com CSS variables"
      contains: "var(--"
    - path: "client/dashboards/NpsSatisfacao/index.vue"
      provides: "NPS dashboard com VLoadingState"
      contains: "VLoadingState"
    - path: "client/dashboards/TxConvSaberMonetizacao/index.vue"
      provides: "TxConv dashboard com VLoadingState e VEmptyState"
      contains: "VLoadingState"
  key_links:
    - from: "client/dashboards/NpsSatisfacao/index.vue"
      to: "client/components/ui/VLoadingState.vue"
      via: "import VLoadingState"
      pattern: "VLoadingState"
    - from: "client/dashboards/TxConvSaberMonetizacao/index.vue"
      to: "client/components/ui/VLoadingState.vue"
      via: "import VLoadingState"
      pattern: "VLoadingState"
    - from: "client/dashboards/TxConvSaberMonetizacao/index.vue"
      to: "client/components/ui/VEmptyState.vue"
      via: "import VEmptyState"
      pattern: "VEmptyState"
---

<objective>
Adicionar estilos globais de hover/focus/responsividade ao components.css e refatorar os 3 dashboards menores (Admin, NpsSatisfacao, TxConvSaberMonetizacao) para usar componentes padronizados e CSS variables.

Purpose: Aplicar os building blocks criados no wave 1 aos dashboards menores (quick wins), estabelecendo o padrao que sera replicado nos dashboards maiores.
Output: components.css expandido + 3 dashboards refatorados com loading/empty states padronizados, responsividade e CSS variables.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-polish-visual/01-CONTEXT.md
@.planning/phases/01-polish-visual/01-UI-SPEC.md
@.planning/phases/01-polish-visual/01-01-SUMMARY.md
@.planning/phases/01-polish-visual/01-02-SUMMARY.md

<interfaces>
<!-- Interfaces from wave 1 plans -->

From client/components/ui/VLoadingState.vue (created in plan 01):
```html
<VLoadingState size="md" />
<!-- Props: size: 'sm'|'md'|'lg' (default 'md') -->
<!-- Renders centered spinner -->
```

From client/components/ui/VEmptyState.vue (created in plan 01):
```html
<VEmptyState icon="database" title="Nenhum dado disponivel" description="Os dados serao exibidos quando estiverem disponiveis." />
<!-- Props: icon: String (Lucide name), title: String (default 'Nenhum dado disponivel'), description: String (optional) -->
```

From client/styles/design-system.css (tokens added in plan 01):
```css
--hover-overlay-subtle: rgba(255, 255, 255, 0.05);
--hover-overlay-light: rgba(255, 255, 255, 0.08);
--hover-overlay-minimal: rgba(255, 255, 255, 0.03);
--focus-ring-color: var(--color-primary);
--focus-ring-width: 2px;
--focus-ring-offset: 2px;
```

From client/styles/components.css (existing media queries):
```css
@media (max-width: 1024px) { .grid-cols-3 { grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 768px) { .scorecards { grid-template-columns: 1fr; } }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expandir components.css com hover, focus-visible e responsividade global</name>
  <files>client/styles/components.css</files>
  <read_first>
    - client/styles/components.css
    - client/styles/design-system.css
    - .planning/phases/01-polish-visual/01-UI-SPEC.md
  </read_first>
  <action>
Adicionar estilos globais de hover, focus e responsividade ao final de components.css. NAO alterar estilos existentes.

1. Adicionar bloco de feedback visual (apos o bloco `/* === Empty State === */`):

```css
/* === Feedback Visual (D-11, D-12) === */

.card:hover {
  background-color: var(--hover-overlay-subtle);
}

.btn:focus-visible {
  outline: var(--focus-ring-width) solid var(--focus-ring-color);
  outline-offset: var(--focus-ring-offset);
}

.input:focus-visible {
  outline: var(--focus-ring-width) solid var(--focus-ring-color);
  outline-offset: var(--focus-ring-offset);
}

.toggle-btn:focus-visible {
  outline: var(--focus-ring-width) solid var(--focus-ring-color);
  outline-offset: var(--focus-ring-offset);
}

.table tbody tr.clickable:hover {
  background-color: var(--hover-overlay-minimal);
  cursor: pointer;
}

.table tbody tr.clickable:active {
  background-color: rgba(255, 255, 255, 0.06);
}
```

2. Expandir o bloco `@media (max-width: 1024px)` existente adicionando regras DENTRO dele:

```css
  .scorecards {
    grid-template-columns: repeat(2, 1fr);
  }

  .card {
    padding: var(--spacing-md);
  }
```

3. Expandir o bloco `@media (max-width: 768px)` existente adicionando regras DENTRO dele:

```css
  .table-container {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
  }

  .table {
    min-width: 600px;
  }

  .main-header {
    flex-direction: column;
    align-items: flex-start;
    gap: var(--spacing-sm);
  }

  .card {
    padding: var(--spacing-sm);
  }
```

ATENCAO: Manter TODAS as regras existentes dentro dos media queries. Apenas adicionar novas regras ao bloco existente.
  </action>
  <verify>
    <automated>cd "C:\Users\Pietro\Desktop\STACKS\dashboards\dashboards-ferraz-piai\.claude\worktrees\cool-leavitt" && grep -c "focus-visible\|hover-overlay-subtle\|hover-overlay-minimal\|clickable\|min-width: 600px\|-webkit-overflow-scrolling" client/styles/components.css</automated>
  </verify>
  <acceptance_criteria>
    - components.css contains `.card:hover` with `background-color: var(--hover-overlay-subtle)`
    - components.css contains `.btn:focus-visible` with `outline: var(--focus-ring-width) solid var(--focus-ring-color)`
    - components.css contains `.input:focus-visible` with same outline pattern
    - components.css contains `.toggle-btn:focus-visible` with same outline pattern
    - components.css contains `.table tbody tr.clickable:hover` with `var(--hover-overlay-minimal)`
    - components.css contains `.table tbody tr.clickable:active` with `rgba(255, 255, 255, 0.06)`
    - The 1024px media query contains `.scorecards` with `repeat(2, 1fr)`
    - The 768px media query contains `.table` with `min-width: 600px`
    - The 768px media query contains `.main-header` with `flex-direction: column`
    - Existing rules (.grid-cols-3, .grid-cols-2, .scorecards 1fr at 768px) remain unchanged
  </acceptance_criteria>
  <done>components.css tem estilos globais de hover/focus-visible e responsividade expandida para tabelas e scorecards</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar Admin, NpsSatisfacao e TxConvSaberMonetizacao</name>
  <files>client/dashboards/Admin/index.vue, client/dashboards/NpsSatisfacao/index.vue, client/dashboards/TxConvSaberMonetizacao/index.vue</files>
  <read_first>
    - client/dashboards/Admin/index.vue
    - client/dashboards/NpsSatisfacao/index.vue
    - client/dashboards/NpsSatisfacao/components/NpsContent.vue
    - client/dashboards/TxConvSaberMonetizacao/index.vue
    - client/components/ui/VLoadingState.vue
    - client/components/ui/VEmptyState.vue
    - .planning/phases/01-polish-visual/01-RESEARCH.md
    - .planning/phases/01-polish-visual/01-UI-SPEC.md
  </read_first>
  <action>
Refatorar 3 dashboards menores para usar componentes padronizados e CSS variables. Usar Edit tool APENAS — nunca Write em arquivos 500+ linhas.

**Admin (client/dashboards/Admin/index.vue):**
1. Substituir cores hardcoded nos scoped styles:
   - `rgba(255,0,0,0.08)` -> `var(--hover-overlay-light)` (tab hover/active states)
   - `#4ade80` -> `var(--color-safe)`
   - `#1a1a1a` -> `var(--bg-inner)`
   - Qualquer outro hex hardcoded -> CSS variable correspondente
2. Adicionar media query responsiva no scoped style:
```css
@media (max-width: 768px) {
  .admin-tabs { flex-direction: column; }
}
```

**NpsSatisfacao (client/dashboards/NpsSatisfacao/index.vue):**
1. Importar VLoadingState: `import VLoadingState from '../../components/ui/VLoadingState.vue'`
2. Importar VEmptyState: `import VEmptyState from '../../components/ui/VEmptyState.vue'`
3. No template, substituir o bloco de loading (provavelmente delega para NpsContent) por:
   - Adicionar `v-if="loading"` com `<VLoadingState size="lg" />`
4. No template, substituir o bloco de error (lines ~84-87 com icone + texto) por:
   - Manter o pattern de erro existente mas usar VEmptyState se nao ha dados:
   - Apos o bloco de erro, antes do conteudo principal, adicionar `v-else-if="!hasData"` com `<VEmptyState icon="bar-chart-2" />`
5. Substituir cores hardcoded nos scoped styles:
   - `#1a1a1a` -> `var(--bg-inner)`
   - `rgba(255,255,255,0.06)` -> `var(--hover-overlay-light)` (se usado como hover)
6. Adicionar media query responsiva no scoped style:
```css
@media (max-width: 768px) {
  .nps-content { padding: var(--spacing-sm); }
}
```

**TxConvSaberMonetizacao (client/dashboards/TxConvSaberMonetizacao/index.vue):**
1. Importar VLoadingState: `import VLoadingState from '../../components/ui/VLoadingState.vue'`
2. Importar VEmptyState: `import VEmptyState from '../../components/ui/VEmptyState.vue'`
3. No template, substituir o bloco de loading custom (spinner + "Carregando dados..." com hardcoded `border-top-color: #ff0000`, linha ~698) por:
   - `<VLoadingState size="lg" />`
4. No template, substituir o bloco de empty state ("Nenhum dado disponivel" com `color: #999`, linhas ~109-150) por:
   - `<VEmptyState icon="bar-chart-2" />`
5. Remover o CSS do spinner custom e do empty state custom nos scoped styles (as linhas que definem `.loading-container` / `.spinner` custom e `.no-data`)
6. Substituir cores hardcoded nos scoped styles:
   - `#ff0000` -> `var(--color-primary)` (se nao faz parte do spinner removido)
   - `#999` -> `var(--text-low)` (se nao faz parte do empty state removido)
7. Adicionar media queries responsivas no scoped style:
```css
@media (max-width: 1024px) {
  .scorecards { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 768px) {
  .scorecards { grid-template-columns: 1fr; }
  .dashboard-container { padding: var(--spacing-sm); }
}
```

CRITICO: Para NpsSatisfacao (512 linhas) e TxConv (707 linhas), usar Edit tool com edits cirurgicos. Nunca Write no arquivo inteiro.
  </action>
  <verify>
    <automated>cd "C:\Users\Pietro\Desktop\STACKS\dashboards\dashboards-ferraz-piai\.claude\worktrees\cool-leavitt" && echo "--- Admin ---" && grep -c "var(--" client/dashboards/Admin/index.vue && echo "--- NPS ---" && grep -c "VLoadingState\|VEmptyState" client/dashboards/NpsSatisfacao/index.vue && echo "--- TxConv ---" && grep -c "VLoadingState\|VEmptyState" client/dashboards/TxConvSaberMonetizacao/index.vue && echo "--- Build ---" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Admin/index.vue does NOT contain `rgba(255,0,0,0.08)` in scoped styles (replaced with var())
    - Admin/index.vue does NOT contain `#4ade80` in scoped styles (replaced with var(--color-safe))
    - Admin/index.vue contains `@media (max-width: 768px)` in scoped styles
    - NpsSatisfacao/index.vue contains `import VLoadingState from`
    - NpsSatisfacao/index.vue contains `<VLoadingState` in template
    - NpsSatisfacao/index.vue contains `@media (max-width: 768px)` in scoped styles
    - TxConvSaberMonetizacao/index.vue contains `import VLoadingState from`
    - TxConvSaberMonetizacao/index.vue contains `import VEmptyState from`
    - TxConvSaberMonetizacao/index.vue contains `<VLoadingState` in template
    - TxConvSaberMonetizacao/index.vue contains `<VEmptyState` in template
    - TxConvSaberMonetizacao/index.vue does NOT contain hardcoded `border-top-color: #ff0000` spinner
    - TxConvSaberMonetizacao/index.vue contains `@media (max-width:` in scoped styles
    - npm run build completes without errors
  </acceptance_criteria>
  <done>Admin, NpsSatisfacao e TxConvSaberMonetizacao refatorados com VLoadingState, VEmptyState, CSS variables e media queries</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Dashboard refactoring is visual-only. No new data paths or API changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T (Tampering) | focus-visible outline | accept | Focus ring is purely visual. No security implication. |
</threat_model>

<verification>
- `npm run build` completa sem erros
- grep confirma VLoadingState/VEmptyState importados nos dashboards
- grep confirma ausencia de cores hardcoded substituidas
- Media queries presentes em todos os 3 dashboards
</verification>

<success_criteria>
- components.css tem hover/focus-visible/responsividade global
- Admin: CSS variables no lugar de hex hardcoded, media query responsiva
- NpsSatisfacao: VLoadingState no lugar de skeleton, media query responsiva
- TxConvSaberMonetizacao: VLoadingState + VEmptyState no lugar de custom, media query responsiva
- Build passa sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/01-polish-visual/01-03-SUMMARY.md`
</output>
