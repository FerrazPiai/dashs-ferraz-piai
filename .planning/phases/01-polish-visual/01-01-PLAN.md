---
phase: 01-polish-visual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/components/ui/VLoadingState.vue
  - client/components/ui/VEmptyState.vue
  - client/styles/design-system.css
autonomous: true
requirements:
  - VIS-03
  - VIS-05

must_haves:
  truths:
    - "VLoadingState renderiza spinner centralizado nos tamanhos sm (16px), md (32px) e lg (48px)"
    - "VEmptyState renderiza icone Lucide + titulo + descricao opcional centralizados"
    - "design-system.css contem tokens de hover overlay, focus ring e spinner-xl"
  artifacts:
    - path: "client/components/ui/VLoadingState.vue"
      provides: "Componente de loading state padronizado"
      contains: "spinner"
    - path: "client/components/ui/VEmptyState.vue"
      provides: "Componente de empty state padronizado"
      contains: "Nenhum dado disponivel"
    - path: "client/styles/design-system.css"
      provides: "Tokens de hover, focus e spinner-xl"
      contains: "--hover-overlay-subtle"
  key_links:
    - from: "client/components/ui/VLoadingState.vue"
      to: "client/styles/design-system.css"
      via: "CSS classes .spinner, .spinner-lg, .spinner-xl"
      pattern: "spinner"
    - from: "client/components/ui/VEmptyState.vue"
      to: "window.lucide"
      via: "lucide.createIcons() em onMounted"
      pattern: "createIcons"
---

<objective>
Criar componentes compartilhados VLoadingState e VEmptyState e expandir design-system.css com tokens de hover, focus e spinner-xl.

Purpose: Estabelecer os building blocks que serao usados em todos os dashboards para substituir loading/empty states custom e para aplicar feedback visual padronizado.
Output: 2 novos componentes Vue + tokens CSS adicionais no design system.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-polish-visual/01-CONTEXT.md
@.planning/phases/01-polish-visual/01-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From client/styles/design-system.css (existing spinner classes):
```css
.spinner {
  display: inline-block;
  width: 16px;
  height: 16px;
  border: 2px solid var(--border-input);
  border-top-color: var(--color-primary);
  border-radius: 50%;
  animation: spin 0.6s linear infinite;
}

.spinner-lg {
  width: 32px;
  height: 32px;
  border-width: 3px;
}
```

From client/components/ui/VScorecard.vue (loading pattern reference):
```html
<div v-if="loading" class="scorecard-value">
  <span class="spinner"></span>
</div>
```

From client/styles/components.css (existing empty-state classes):
```css
.empty-state { text-align: center; padding: var(--spacing-2xl); color: var(--text-muted); }
.empty-state-icon { width: 48px; height: 48px; margin: 0 auto var(--spacing-md); color: var(--text-lowest); }
.empty-state-text { font-size: var(--font-size-lg); color: var(--text-muted); }
```

Lucide Icons pattern (CDN, not npm):
```html
<i data-lucide="icon-name"></i>
<!-- then call window.lucide.createIcons() in onMounted -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expandir design-system.css com tokens de hover, focus e spinner-xl</name>
  <files>client/styles/design-system.css</files>
  <read_first>
    - client/styles/design-system.css
    - .planning/phases/01-polish-visual/01-UI-SPEC.md
  </read_first>
  <action>
Adicionar novos tokens CSS e a classe .spinner-xl em client/styles/design-system.css.

1. Dentro do bloco `:root`, apos o grupo `/* === Transicoes === */`, adicionar novo grupo:

```css
/* === Hover & Focus === */
--hover-overlay-subtle: rgba(255, 255, 255, 0.05);
--hover-overlay-light: rgba(255, 255, 255, 0.08);
--hover-overlay-minimal: rgba(255, 255, 255, 0.03);
--focus-ring-color: var(--color-primary);
--focus-ring-width: 2px;
--focus-ring-offset: 2px;
```

2. Apos a classe `.spinner-lg` existente (que termina com `border-width: 3px; }`), adicionar:

```css
.spinner-xl {
  width: 48px;
  height: 48px;
  border-width: 4px;
}
```

NAO alterar nenhum token existente. Apenas adicionar.
  </action>
  <verify>
    <automated>cd "C:\Users\Pietro\Desktop\STACKS\dashboards\dashboards-ferraz-piai\.claude\worktrees\cool-leavitt" && grep -c "hover-overlay-subtle\|hover-overlay-light\|hover-overlay-minimal\|focus-ring-color\|focus-ring-width\|focus-ring-offset\|spinner-xl" client/styles/design-system.css</automated>
  </verify>
  <acceptance_criteria>
    - client/styles/design-system.css contains `--hover-overlay-subtle: rgba(255, 255, 255, 0.05)`
    - client/styles/design-system.css contains `--hover-overlay-light: rgba(255, 255, 255, 0.08)`
    - client/styles/design-system.css contains `--hover-overlay-minimal: rgba(255, 255, 255, 0.03)`
    - client/styles/design-system.css contains `--focus-ring-color: var(--color-primary)`
    - client/styles/design-system.css contains `--focus-ring-width: 2px`
    - client/styles/design-system.css contains `--focus-ring-offset: 2px`
    - client/styles/design-system.css contains `.spinner-xl` with `width: 48px` and `height: 48px` and `border-width: 4px`
    - Existing tokens (.spinner, .spinner-lg, all :root vars) remain unchanged
  </acceptance_criteria>
  <done>design-system.css tem 6 novos tokens de hover/focus e classe .spinner-xl</done>
</task>

<task type="auto">
  <name>Task 2: Criar VLoadingState.vue</name>
  <files>client/components/ui/VLoadingState.vue</files>
  <read_first>
    - client/styles/design-system.css
    - client/components/ui/VScorecard.vue
    - .planning/phases/01-polish-visual/01-UI-SPEC.md
  </read_first>
  <action>
Criar client/components/ui/VLoadingState.vue conforme contrato da UI-SPEC.

Componente Vue SFC com `<script setup>`, scoped styles.

Props:
- `size`: String, default 'md', validator que aceita ['sm', 'md', 'lg']

Template:
```html
<div class="loading-state">
  <span :class="spinnerClass"></span>
</div>
```

Script:
- import { computed } from 'vue'
- computed `spinnerClass` retorna:
  - size === 'sm' -> 'spinner'
  - size === 'md' -> 'spinner spinner-lg'
  - size === 'lg' -> 'spinner spinner-xl'

Scoped styles:
```css
.loading-state {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--spacing-xl);
}
```

Sem texto (D-02 — spinner only). Sem skeleton.
  </action>
  <verify>
    <automated>cd "C:\Users\Pietro\Desktop\STACKS\dashboards\dashboards-ferraz-piai\.claude\worktrees\cool-leavitt" && grep -c "spinner\|loading-state\|defineProps" client/components/ui/VLoadingState.vue</automated>
  </verify>
  <acceptance_criteria>
    - client/components/ui/VLoadingState.vue exists
    - File contains `defineProps` with `size` prop having `default: 'md'`
    - File contains `validator` that checks `['sm', 'md', 'lg']`
    - File contains `.loading-state` class with `display: flex` and `align-items: center` and `justify-content: center` and `padding: var(--spacing-xl)`
    - File contains computed that maps sm->spinner, md->spinner spinner-lg, lg->spinner spinner-xl
    - File does NOT contain text like "Carregando" or skeleton
    - File uses `<script setup>` and `<style scoped>`
  </acceptance_criteria>
  <done>VLoadingState.vue criado com prop size (sm/md/lg) renderizando spinner centralizado</done>
</task>

<task type="auto">
  <name>Task 3: Criar VEmptyState.vue</name>
  <files>client/components/ui/VEmptyState.vue</files>
  <read_first>
    - client/styles/design-system.css
    - client/components/ui/VDataTable.vue
    - .planning/phases/01-polish-visual/01-UI-SPEC.md
  </read_first>
  <action>
Criar client/components/ui/VEmptyState.vue conforme contrato da UI-SPEC.

Componente Vue SFC com `<script setup>`, scoped styles.

Props:
- `icon`: String (nome do icone Lucide, ex: 'database'), sem default (opcional)
- `title`: String, default 'Nenhum dado disponivel'
- `description`: String, default null (opcional)

Template:
```html
<div class="empty-state-container">
  <i v-if="icon" :data-lucide="icon" class="empty-state-icon"></i>
  <div class="empty-state-title">{{ title }}</div>
  <div v-if="description" class="empty-state-description">{{ description }}</div>
</div>
```

Script:
- import { onMounted, nextTick } from 'vue'
- onMounted: `await nextTick(); if (window.lucide) window.lucide.createIcons()`

Scoped styles:
```css
.empty-state-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--spacing-xl);
}

.empty-state-icon {
  width: 32px;
  height: 32px;
  color: var(--text-lowest);
  margin-bottom: var(--spacing-md);
}

.empty-state-title {
  font-size: var(--font-size-lg);
  color: var(--text-low);
  text-align: center;
}

.empty-state-description {
  font-size: var(--font-size-base);
  color: var(--text-muted);
  text-align: center;
  margin-top: var(--spacing-sm);
}
```

Sem botao de acao (D-04).
  </action>
  <verify>
    <automated>cd "C:\Users\Pietro\Desktop\STACKS\dashboards\dashboards-ferraz-piai\.claude\worktrees\cool-leavitt" && grep -c "empty-state-container\|defineProps\|data-lucide\|Nenhum dado disponivel" client/components/ui/VEmptyState.vue</automated>
  </verify>
  <acceptance_criteria>
    - client/components/ui/VEmptyState.vue exists
    - File contains `defineProps` with `icon` (String), `title` (String, default 'Nenhum dado disponivel'), `description` (String)
    - File contains `data-lucide` attribute binding to `icon` prop
    - File contains `window.lucide` and `createIcons()` in onMounted
    - File contains `.empty-state-icon` with `width: 32px` and `color: var(--text-lowest)` and `margin-bottom: var(--spacing-md)`
    - File contains `.empty-state-title` with `font-size: var(--font-size-lg)` and `color: var(--text-low)`
    - File contains `.empty-state-description` with `font-size: var(--font-size-base)` and `color: var(--text-muted)` and `margin-top: var(--spacing-sm)`
    - File does NOT contain `<button>` or `btn` (no CTA per D-04)
    - File uses `<script setup>` and `<style scoped>`
  </acceptance_criteria>
  <done>VEmptyState.vue criado com props icon/title/description, sem botao de acao</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan creates UI-only components with no data input/output beyond props |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | VEmptyState icon prop | accept | Icon name is a string used in data-lucide attribute. Lucide renders only known icons; unknown names are ignored. No user-supplied data reaches this prop (developer-controlled). |
</threat_model>

<verification>
- `npm run build` completa sem erros
- grep confirma que VLoadingState.vue, VEmptyState.vue e novos tokens existem
- Componentes seguem convencoes Vue do projeto (script setup, scoped styles, props tipados)
</verification>

<success_criteria>
- VLoadingState.vue renderiza spinner centralizado em 3 tamanhos (sm/md/lg)
- VEmptyState.vue renderiza icone + titulo + descricao opcional, sem botao de acao
- design-system.css tem 6 novos tokens (hover/focus) e classe .spinner-xl
- Nenhum arquivo existente foi quebrado (build passa)
</success_criteria>

<output>
After completion, create `.planning/phases/01-polish-visual/01-01-SUMMARY.md`
</output>
