---
phase: 01-polish-visual
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - client/dashboards/FechamentoMensal/index.vue
  - client/dashboards/DreFluxoCaixa/index.vue
  - client/dashboards/MarketingVendas/index.vue
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-05

must_haves:
  truths:
    - "FechamentoMensal usa VLoadingState no lugar de skeleton shimmer"
    - "DreFluxoCaixa usa VLoadingState no lugar de spinner custom com hex hardcoded"
    - "MarketingVendas usa CSS variables no lugar de cores hardcoded"
    - "Todos os 3 dashboards tem media queries para 1024px e 768px"
  artifacts:
    - path: "client/dashboards/FechamentoMensal/index.vue"
      provides: "Fechamento Mensal com VLoadingState e media queries"
      contains: "VLoadingState"
    - path: "client/dashboards/DreFluxoCaixa/index.vue"
      provides: "DRE Fluxo de Caixa com VLoadingState e CSS variables"
      contains: "VLoadingState"
    - path: "client/dashboards/MarketingVendas/index.vue"
      provides: "Marketing Vendas com CSS variables e media queries"
      contains: "var(--"
  key_links:
    - from: "client/dashboards/FechamentoMensal/index.vue"
      to: "client/components/ui/VLoadingState.vue"
      via: "import VLoadingState"
      pattern: "VLoadingState"
    - from: "client/dashboards/DreFluxoCaixa/index.vue"
      to: "client/components/ui/VLoadingState.vue"
      via: "import VLoadingState"
      pattern: "VLoadingState"
---

<objective>
Refatorar FechamentoMensal, DreFluxoCaixa e MarketingVendas para usar componentes padronizados, CSS variables e media queries responsivas.

Purpose: Estender o padrao visual estabelecido nos dashboards menores (plan 03) para os dashboards de porte medio, eliminando loading states custom, cores hardcoded e adicionando responsividade.
Output: 3 dashboards refatorados com loading states padronizados, CSS variables e media queries.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-polish-visual/01-CONTEXT.md
@.planning/phases/01-polish-visual/01-UI-SPEC.md
@.planning/phases/01-polish-visual/01-03-SUMMARY.md

<interfaces>
<!-- Interfaces established in previous plans -->

VLoadingState usage:
```html
<VLoadingState size="lg" />
<!-- import VLoadingState from '../../components/ui/VLoadingState.vue' -->
```

VEmptyState usage:
```html
<VEmptyState icon="bar-chart-2" />
<!-- import VEmptyState from '../../components/ui/VEmptyState.vue' -->
```

CSS tokens available:
```css
--hover-overlay-subtle: rgba(255, 255, 255, 0.05);
--hover-overlay-light: rgba(255, 255, 255, 0.08);
--hover-overlay-minimal: rgba(255, 255, 255, 0.03);
--bg-inner: #1a1a1a;
--bg-card: #141414;
--bg-body: #0d0d0d;
--text-low: #999999;
--text-muted: #888888;
--text-lowest: #666666;
--color-safe: #22c55e;
--color-care: #fbbf24;
--color-danger: #ef4444;
--color-primary: #ff0000;
--border-card: #2a2a2a;
--border-row: #1f1f1f;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar FechamentoMensal — loading state e responsividade</name>
  <files>client/dashboards/FechamentoMensal/index.vue</files>
  <read_first>
    - client/dashboards/FechamentoMensal/index.vue
    - .planning/phases/01-polish-visual/01-RESEARCH.md
    - .planning/phases/01-polish-visual/01-UI-SPEC.md
  </read_first>
  <action>
Refatorar FechamentoMensal/index.vue. CRITICO: arquivo tem 1570 linhas — usar APENAS Edit tool com edits cirurgicos, nunca Write no arquivo inteiro.

1. Importar VLoadingState no script setup:
   `import VLoadingState from '../../components/ui/VLoadingState.vue'`

2. No template, localizar o bloco de skeleton shimmer (loading state com `@keyframes shimmer`, provavelmente linha ~1473) e substituir o conteudo do loading por:
   `<VLoadingState size="lg" />`

3. Remover o CSS do `@keyframes shimmer` e classes associadas do skeleton (`.skeleton-*` classes) nos scoped styles. Manter TODOS os outros estilos intactos.

4. Substituir cores hardcoded nao-semanticas nos scoped styles:
   - `#1a1a1a` -> `var(--bg-inner)`
   - `#222` -> `var(--bg-toggle-active)` (2a2a2a e o mais proximo, #222 e aceitavel como var(--bg-inner) tambem)
   - `#141414` -> `var(--bg-card)`
   NAO substituir: cores semanticas de heatmap (rgba green/yellow/red) que servem proposito de data visualization. Essas permanecem como estao.

5. Adicionar media queries responsivas no scoped style (se nao existirem):
```css
@media (max-width: 1024px) {
  .scorecards { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 768px) {
  .scorecards { grid-template-columns: 1fr; }
  .dashboard-container { padding: var(--spacing-sm); }
}
```

6. Garantir que tabelas grandes (heatmap) estao dentro de container com `overflow-x: auto` (D-07). Se nao estiverem, envolver com `<div style="overflow-x: auto">`.
  </action>
  <verify>
    <automated>cd "C:\Users\Pietro\Desktop\STACKS\dashboards\dashboards-ferraz-piai\.claude\worktrees\cool-leavitt" && echo "--- VLoadingState ---" && grep -c "VLoadingState" client/dashboards/FechamentoMensal/index.vue && echo "--- Shimmer removed ---" && grep -c "@keyframes shimmer" client/dashboards/FechamentoMensal/index.vue && echo "--- Media queries ---" && grep -c "@media" client/dashboards/FechamentoMensal/index.vue</automated>
  </verify>
  <acceptance_criteria>
    - FechamentoMensal/index.vue contains `import VLoadingState from`
    - FechamentoMensal/index.vue contains `<VLoadingState` in template
    - FechamentoMensal/index.vue does NOT contain `@keyframes shimmer`
    - FechamentoMensal/index.vue does NOT contain `.skeleton-` classes (skeleton CSS removed)
    - FechamentoMensal/index.vue contains `@media (max-width:` in scoped styles (at least 2 breakpoints)
    - Non-semantic hardcoded hex values `#1a1a1a` and `#141414` replaced with CSS variables in scoped styles
    - Heatmap rgba colors (green/yellow/red semantic) remain unchanged
    - File line count is within ~50 lines of original (only removed skeleton CSS, added media queries)
  </acceptance_criteria>
  <done>FechamentoMensal usa VLoadingState (sem shimmer skeleton), CSS variables, overflow-x em tabelas e media queries responsivas</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar DreFluxoCaixa e MarketingVendas</name>
  <files>client/dashboards/DreFluxoCaixa/index.vue, client/dashboards/MarketingVendas/index.vue</files>
  <read_first>
    - client/dashboards/DreFluxoCaixa/index.vue
    - client/dashboards/MarketingVendas/index.vue
    - client/dashboards/MarketingVendas/components/MvSectionTable.vue
    - .planning/phases/01-polish-visual/01-RESEARCH.md
    - .planning/phases/01-polish-visual/01-UI-SPEC.md
  </read_first>
  <action>
Refatorar DreFluxoCaixa e MarketingVendas. CRITICO: DreFluxoCaixa tem 1244 linhas — usar APENAS Edit tool com edits cirurgicos.

**DreFluxoCaixa (client/dashboards/DreFluxoCaixa/index.vue):**

1. Importar VLoadingState no script setup:
   `import VLoadingState from '../../components/ui/VLoadingState.vue'`

2. No template, localizar o loading state custom (spinner com hardcoded red border-top ~linha 1077) e substituir por:
   `<VLoadingState size="lg" />`

3. Remover CSS do spinner custom nos scoped styles (a classe local que define o spinner 40px com `border-top-color: #ff0000`). Manter o spinner usado dentro de KPI cards se for diferente.

4. Substituir cores hardcoded nao-semanticas nos scoped styles:
   - `#ff0000` (se usado fora de spinner removido) -> `var(--color-primary)`
   - `#111` (filter bg) -> `var(--bg-body)` ou `var(--bg-inner)` (o que for mais proximo visualmente)
   - KPI badge rgba colors: manter se servem proposito semantico (safe/care/danger)

5. DreFluxoCaixa JA tem media queries (1400px, 1024px, 640px). Verificar se cobrem o minimo de 1024px e 768px. Se 768px nao existir, adicionar:
```css
@media (max-width: 768px) {
  .kpi-grid { grid-template-columns: 1fr; }
}
```

**MarketingVendas (client/dashboards/MarketingVendas/index.vue):**

MarketingVendas tem 299 linhas — pode usar Write se necessario, mas Edit e preferivel.

1. Importar VLoadingState: `import VLoadingState from '../../components/ui/VLoadingState.vue'`
   Importar VEmptyState: `import VEmptyState from '../../components/ui/VEmptyState.vue'`

2. Verificar se ha loading state custom no template. Se delega para MvSectionTable/MvListagemTable children, adicionar loading state no nivel do index.vue antes de renderizar as tabelas:
   - Se `loading` ref existe: `<VLoadingState v-if="loading" size="lg" />`
   - Se nao existe, verificar o que `useDashboardData` retorna

3. Adicionar empty state: `<VEmptyState v-else-if="!data" icon="table-2" />` antes do conteudo principal

4. Substituir cores hardcoded nos scoped styles:
   - `#14b8a6` -> mantem se e cor semantica de tier (nao e parte do design system)
   - `#888` -> `var(--text-muted)`
   - `#a855f7` -> `var(--chart-color-5)` (roxo do chart palette)
   - `#666` -> `var(--text-lowest)`

5. Adicionar media queries responsivas:
```css
@media (max-width: 1024px) {
  .section-grid { grid-template-columns: 1fr; }
}

@media (max-width: 768px) {
  .dashboard-container { padding: var(--spacing-sm); }
}
```
  </action>
  <verify>
    <automated>cd "C:\Users\Pietro\Desktop\STACKS\dashboards\dashboards-ferraz-piai\.claude\worktrees\cool-leavitt" && echo "--- DRE VLoadingState ---" && grep -c "VLoadingState" client/dashboards/DreFluxoCaixa/index.vue && echo "--- DRE Media ---" && grep -c "@media" client/dashboards/DreFluxoCaixa/index.vue && echo "--- MV VLoadingState ---" && grep -c "VLoadingState\|VEmptyState" client/dashboards/MarketingVendas/index.vue && echo "--- MV Media ---" && grep -c "@media" client/dashboards/MarketingVendas/index.vue && echo "--- Build ---" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DreFluxoCaixa/index.vue contains `import VLoadingState from`
    - DreFluxoCaixa/index.vue contains `<VLoadingState` in template
    - DreFluxoCaixa/index.vue does NOT contain hardcoded spinner with `border-top-color: #ff0000` in scoped styles
    - DreFluxoCaixa/index.vue contains `@media (max-width: 768px)` or `@media (max-width: 640px)` in scoped styles
    - MarketingVendas/index.vue contains `import VLoadingState from`
    - MarketingVendas/index.vue contains `<VLoadingState` in template
    - MarketingVendas/index.vue has `#888` replaced with `var(--text-muted)` in scoped styles
    - MarketingVendas/index.vue has `#666` replaced with `var(--text-lowest)` in scoped styles
    - MarketingVendas/index.vue contains `@media (max-width:` in scoped styles
    - npm run build completes without errors
  </acceptance_criteria>
  <done>DreFluxoCaixa e MarketingVendas refatorados com VLoadingState, CSS variables e media queries</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Dashboard refactoring is visual-only. No new data paths or API changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | I (Info Disclosure) | DreFluxoCaixa KPI values | accept | No change to data handling. Only visual CSS refactoring. Financial data display unchanged. |
</threat_model>

<verification>
- `npm run build` completa sem erros
- VLoadingState importado em FechamentoMensal, DreFluxoCaixa e MarketingVendas
- Skeleton shimmer removido de FechamentoMensal
- Spinner custom hardcoded removido de DreFluxoCaixa
- Media queries responsivas presentes em todos os 3 dashboards
</verification>

<success_criteria>
- FechamentoMensal: VLoadingState (sem shimmer), CSS variables, overflow-x em tabelas, media queries
- DreFluxoCaixa: VLoadingState (sem spinner hardcoded), CSS variables, media queries expandidas
- MarketingVendas: VLoadingState + VEmptyState, CSS variables, media queries
- Build passa sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/01-polish-visual/01-04-SUMMARY.md`
</output>
