---
phase: 01-polish-visual
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/composables/useChartDefaults.js
  - client/components/charts/VBarChart.vue
  - client/components/charts/VLineChart.vue
autonomous: true
requirements:
  - VIS-04

must_haves:
  truths:
    - "useChartDefaults.js e a unica fonte de verdade para cores, tooltips, fonts e grid de graficos"
    - "VBarChart.vue usa getChartDefaults() e getChartColors() em vez de valores hardcoded"
    - "VLineChart.vue usa getChartDefaults() em vez de valores hardcoded"
    - "Graficos renderizam visualmente identicos antes e depois da refatoracao"
  artifacts:
    - path: "client/composables/useChartDefaults.js"
      provides: "Fonte de verdade expandida para configuracao de graficos"
      exports: ["getChartDefaults", "getChartColors", "getSemanticColors", "getBarDefaults", "getLineDefaults", "getDatalabelDefaults", "createGradient"]
    - path: "client/components/charts/VBarChart.vue"
      provides: "Bar chart usando composable centralizado"
      contains: "useChartDefaults"
    - path: "client/components/charts/VLineChart.vue"
      provides: "Line chart usando composable centralizado"
      contains: "useChartDefaults"
  key_links:
    - from: "client/components/charts/VBarChart.vue"
      to: "client/composables/useChartDefaults.js"
      via: "import { getChartDefaults, getChartColors, getBarDefaults, getDatalabelDefaults }"
      pattern: "getChartDefaults|getChartColors|getBarDefaults"
    - from: "client/components/charts/VLineChart.vue"
      to: "client/composables/useChartDefaults.js"
      via: "import { getChartDefaults, getLineDefaults, getDatalabelDefaults }"
      pattern: "getChartDefaults|getLineDefaults"
---

<objective>
Centralizar configuracao de graficos no composable useChartDefaults.js e refatorar VBarChart e VLineChart para usarem o composable como unica fonte de verdade.

Purpose: Eliminar duplicacao de cores, tooltip styles e font configs entre chart components (per D-08, D-09, D-10). Garantir que todos os graficos usem a mesma paleta e estilos.
Output: Composable expandido + 2 chart components refatorados.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-polish-visual/01-CONTEXT.md
@.planning/phases/01-polish-visual/01-UI-SPEC.md

<interfaces>
<!-- Current state of files being modified -->

From client/composables/useChartDefaults.js (current exports):
```js
export function getChartDefaults(overrides = {}) { ... }  // tooltip bg #141414, grid rgba(255,255,255,0.03)
export function getChartColors() { ... }  // 8-color array
export function getSemanticColors() { ... }  // safe/care/danger/info/primary/neutral
export function createGradient(ctx, color, height = 400) { ... }
export default { getChartDefaults, getChartColors, getSemanticColors, createGradient }
```

From client/components/charts/VBarChart.vue (hardcoded values to centralize):
- Tooltip: bg #141414, titleColor #fff, bodyColor #ccc, borderColor #333, borderWidth 1, padding 12
- Grid: rgba(255,255,255,0.03), drawBorder false
- Ticks: color #666, font size 11
- Legend: color #ccc, font size 12, padding 15
- Colors array: ['#22c55e','#f97316','#fbbf24','#ff0000','#a855f7','#84cc16','#ec4899','#999999']
- Datalabels: horizontal {11px bold}, vertical {10px 600 weight}
- Bar defaults: borderWidth 0, borderRadius 4

From client/components/charts/VLineChart.vue (hardcoded values to centralize):
- Same tooltip/grid/tick values
- Color default: '#ff0000'
- Point: radius 4, hoverRadius 6, borderColor '#ffffff', borderWidth 2
- Line: tension 0.4, borderWidth 2
- Datalabels: 12px bold
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expandir useChartDefaults.js com defaults de bar, line e datalabels</name>
  <files>client/composables/useChartDefaults.js</files>
  <read_first>
    - client/composables/useChartDefaults.js
    - client/components/charts/VBarChart.vue
    - client/components/charts/VLineChart.vue
    - .planning/phases/01-polish-visual/01-UI-SPEC.md
  </read_first>
  <action>
Expandir client/composables/useChartDefaults.js adicionando 3 novas funcoes exportadas. Manter TODAS as funcoes existentes intactas.

1. Atualizar getChartDefaults() — alterar APENAS o tooltip para alinhar com UI-SPEC:
   - tooltip.backgroundColor: '#1a1a1a' (era '#141414' — per UI-SPEC contrato)
   - tooltip.titleFont: { family: "'Ubuntu', 'Segoe UI', sans-serif", size: 12, weight: '600' } (era size 13)
   - Demais valores permanecem identicos

2. Adicionar nova funcao `getBarDefaults()`:
```js
/**
 * Get bar chart specific defaults
 * @returns {object} Bar-specific config
 */
export function getBarDefaults() {
  return {
    borderWidth: 0,
    borderRadius: 4
  }
}
```

3. Adicionar nova funcao `getLineDefaults()`:
```js
/**
 * Get line chart specific defaults
 * @returns {object} Line-specific config
 */
export function getLineDefaults() {
  return {
    tension: 0.4,
    borderWidth: 2,
    pointRadius: 4,
    pointHoverRadius: 6,
    pointBorderColor: '#ffffff',
    pointBorderWidth: 2
  }
}
```

4. Adicionar nova funcao `getDatalabelDefaults(orientation)`:
```js
/**
 * Get datalabel plugin defaults
 * @param {'vertical'|'horizontal'} orientation - Chart orientation
 * @returns {object} Datalabel config
 */
export function getDatalabelDefaults(orientation = 'vertical') {
  const base = {
    color: '#ffffff',
    font: {
      family: "'Ubuntu', 'Segoe UI', sans-serif",
      size: 10,
      weight: 'bold'
    }
  }

  if (orientation === 'horizontal') {
    return { ...base, font: { ...base.font, size: 11 }, anchor: 'end', align: 'right', offset: 8 }
  }

  return { ...base, anchor: 'end', align: 'top', offset: 4 }
}
```

5. Atualizar o default export para incluir as novas funcoes:
```js
export default {
  getChartDefaults,
  getChartColors,
  getSemanticColors,
  getBarDefaults,
  getLineDefaults,
  getDatalabelDefaults,
  createGradient
}
```

6. Exportar `mergeDeep` como funcao nomeada para uso nos chart components:
   Mudar `function mergeDeep(target, source)` para `export function mergeDeep(target, source)`
  </action>
  <verify>
    <automated>cd "C:\Users\Pietro\Desktop\STACKS\dashboards\dashboards-ferraz-piai\.claude\worktrees\cool-leavitt" && grep -c "getBarDefaults\|getLineDefaults\|getDatalabelDefaults\|export function mergeDeep" client/composables/useChartDefaults.js</automated>
  </verify>
  <acceptance_criteria>
    - useChartDefaults.js contains `export function getBarDefaults()`
    - useChartDefaults.js contains `export function getLineDefaults()`
    - useChartDefaults.js contains `export function getDatalabelDefaults(`
    - useChartDefaults.js contains `export function mergeDeep(`
    - getBarDefaults returns object with `borderWidth: 0` and `borderRadius: 4`
    - getLineDefaults returns object with `tension: 0.4` and `pointRadius: 4` and `pointBorderColor: '#ffffff'`
    - getDatalabelDefaults('horizontal') returns `size: 11` and `align: 'right'`
    - getDatalabelDefaults('vertical') returns `size: 10` and `align: 'top'`
    - getChartDefaults tooltip.backgroundColor is '#1a1a1a'
    - getChartDefaults tooltip.titleFont.size is 12
    - All existing exports (getChartDefaults, getChartColors, getSemanticColors, createGradient) still present
    - Default export includes all 7 functions
  </acceptance_criteria>
  <done>useChartDefaults.js expandido com getBarDefaults, getLineDefaults, getDatalabelDefaults e mergeDeep exportado</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar VBarChart.vue para usar useChartDefaults</name>
  <files>client/components/charts/VBarChart.vue</files>
  <read_first>
    - client/components/charts/VBarChart.vue
    - client/composables/useChartDefaults.js
  </read_first>
  <action>
Refatorar VBarChart.vue para eliminar todos os valores hardcoded de cores, tooltips, grid e datalabels, usando o composable como unica fonte.

1. Adicionar import no topo do script setup:
```js
import { getChartDefaults, getChartColors, getBarDefaults, getDatalabelDefaults, mergeDeep } from '../../composables/useChartDefaults.js'
```

2. Substituir o bloco `const defaultOptions = { ... }` (linhas ~62-117) por:
```js
const baseDefaults = getChartDefaults({
  plugins: {
    legend: {
      display: props.datasets.length > 1,
      position: 'top'
    }
  },
  scales: {
    x: { stacked: props.stacked },
    y: { stacked: props.stacked }
  }
})

const defaultOptions = {
  ...baseDefaults,
  indexAxis: props.horizontal ? 'y' : 'x'
}
```

3. Substituir o bloco de datalabels (linhas ~120-139) por:
```js
if (props.datalabels && window.ChartDataLabels) {
  defaultOptions.plugins.datalabels = getDatalabelDefaults(props.horizontal ? 'horizontal' : 'vertical')
}
```

4. Substituir o merge de options por:
```js
const mergedOptions = mergeDeep(defaultOptions, props.options)
```

5. Substituir o array `const defaultColors = [...]` (linhas ~157-166) por:
```js
const defaultColors = getChartColors()
```

6. Na criacao de processedDatasets, usar getBarDefaults():
```js
const barDefaults = getBarDefaults()
const processedDatasets = props.datasets.map((dataset, index) => ({
  ...dataset,
  backgroundColor: dataset.backgroundColor || defaultColors[index % defaultColors.length],
  borderColor: dataset.borderColor || defaultColors[index % defaultColors.length],
  borderWidth: dataset.borderWidth ?? barDefaults.borderWidth,
  borderRadius: dataset.borderRadius ?? barDefaults.borderRadius
}))
```

NAO alterar: props, template, watch, onMounted, onBeforeUnmount, scoped styles.
  </action>
  <verify>
    <automated>cd "C:\Users\Pietro\Desktop\STACKS\dashboards\dashboards-ferraz-piai\.claude\worktrees\cool-leavitt" && grep -c "getChartDefaults\|getChartColors\|getBarDefaults\|getDatalabelDefaults\|useChartDefaults" client/components/charts/VBarChart.vue</automated>
  </verify>
  <acceptance_criteria>
    - VBarChart.vue contains `import { getChartDefaults, getChartColors, getBarDefaults, getDatalabelDefaults, mergeDeep } from '../../composables/useChartDefaults.js'`
    - VBarChart.vue does NOT contain hardcoded tooltip colors (`'#141414'` or `'#ffffff'` or `'#cccccc'` or `'#333333'` inside tooltip config)
    - VBarChart.vue does NOT contain hardcoded color array `['#22c55e',`
    - VBarChart.vue contains `getChartDefaults(` call
    - VBarChart.vue contains `getChartColors()` call
    - VBarChart.vue contains `getBarDefaults()` call
    - VBarChart.vue contains `getDatalabelDefaults(` call
    - VBarChart.vue contains `mergeDeep(defaultOptions, props.options)`
    - Props, template, watch, onMounted, onBeforeUnmount remain unchanged
  </acceptance_criteria>
  <done>VBarChart.vue usa useChartDefaults como unica fonte de verdade para cores, tooltips, grid e datalabels</done>
</task>

<task type="auto">
  <name>Task 3: Refatorar VLineChart.vue para usar useChartDefaults</name>
  <files>client/components/charts/VLineChart.vue</files>
  <read_first>
    - client/components/charts/VLineChart.vue
    - client/composables/useChartDefaults.js
  </read_first>
  <action>
Refatorar VLineChart.vue para eliminar todos os valores hardcoded de tooltips, grid e point styling, usando o composable como unica fonte.

1. Adicionar import no topo do script setup:
```js
import { getChartDefaults, getLineDefaults, getDatalabelDefaults, createGradient, mergeDeep } from '../../composables/useChartDefaults.js'
```

2. Substituir o bloco `const defaultOptions = { ... }` (linhas ~66-115) por:
```js
const defaultOptions = getChartDefaults({
  plugins: {
    legend: { display: false },
    tooltip: {
      displayColors: false,
      callbacks: {
        label: (context) => {
          return `${context.parsed.y.toFixed(2)}%`
        }
      }
    }
  },
  scales: {
    y: {
      ticks: {
        callback: (value) => `${value}%`
      }
    }
  }
})
```

3. Substituir o bloco de datalabels (linhas ~118-132) por:
```js
if (props.datalabels && window.ChartDataLabels) {
  defaultOptions.plugins.datalabels = {
    ...getDatalabelDefaults('vertical'),
    font: { ...getDatalabelDefaults('vertical').font, size: 12 },
    offset: 6,
    formatter: (value) => {
      return value ? `${parseFloat(value).toFixed(1)}%` : ''
    }
  }
}
```

4. Substituir o merge de options por:
```js
const mergedOptions = mergeDeep(defaultOptions, props.options)
```

5. No bloco de criacao do gradient (linhas ~57-63), usar `createGradient`:
```js
let backgroundColor = props.color
if (props.gradient) {
  backgroundColor = createGradient(ctx, props.color, 400)
}
```

6. Na criacao do dataset, usar getLineDefaults():
```js
const lineDefaults = getLineDefaults()
// ...
datasets: [
  {
    label: props.label,
    data: props.data,
    borderColor: props.color,
    backgroundColor: backgroundColor,
    borderWidth: lineDefaults.borderWidth,
    fill: props.gradient,
    tension: lineDefaults.tension,
    pointRadius: lineDefaults.pointRadius,
    pointHoverRadius: lineDefaults.pointHoverRadius,
    pointBackgroundColor: props.color,
    pointBorderColor: lineDefaults.pointBorderColor,
    pointBorderWidth: lineDefaults.pointBorderWidth
  }
]
```

NAO alterar: props, template, watch, onMounted, onBeforeUnmount, scoped styles.
  </action>
  <verify>
    <automated>cd "C:\Users\Pietro\Desktop\STACKS\dashboards\dashboards-ferraz-piai\.claude\worktrees\cool-leavitt" && grep -c "getChartDefaults\|getLineDefaults\|getDatalabelDefaults\|createGradient\|useChartDefaults" client/components/charts/VLineChart.vue</automated>
  </verify>
  <acceptance_criteria>
    - VLineChart.vue contains `import { getChartDefaults, getLineDefaults, getDatalabelDefaults, createGradient, mergeDeep } from '../../composables/useChartDefaults.js'`
    - VLineChart.vue does NOT contain hardcoded tooltip colors (`'#141414'` or `'#ffffff'` or `'#cccccc'` inside tooltip config)
    - VLineChart.vue contains `getChartDefaults(` call
    - VLineChart.vue contains `getLineDefaults()` call
    - VLineChart.vue contains `getDatalabelDefaults(` call
    - VLineChart.vue contains `createGradient(` call
    - VLineChart.vue contains `mergeDeep(defaultOptions, props.options)`
    - Props, template, watch, onMounted, onBeforeUnmount remain unchanged
  </acceptance_criteria>
  <done>VLineChart.vue usa useChartDefaults como unica fonte de verdade para cores, tooltips, grid, point styling e datalabels</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Chart components receive data from parent dashboards via props — no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | T (Tampering) | Chart data props | accept | Props come from server-fetched data via useDashboardData. No new attack surface — refactoring only moves config to composable. |
</threat_model>

<verification>
- `npm run build` completa sem erros
- VBarChart e VLineChart importam de useChartDefaults.js
- Nenhum valor hardcoded de cores/tooltip/grid permanece nos chart components
- useChartDefaults.js exporta 7 funcoes (getChartDefaults, getChartColors, getSemanticColors, getBarDefaults, getLineDefaults, getDatalabelDefaults, createGradient) + mergeDeep
</verification>

<success_criteria>
- useChartDefaults.js e a unica fonte de verdade: getBarDefaults, getLineDefaults, getDatalabelDefaults adicionados
- VBarChart.vue zero valores hardcoded de cores/tooltips/grid — tudo via composable
- VLineChart.vue zero valores hardcoded de cores/tooltips/grid — tudo via composable
- Build passa sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/01-polish-visual/01-02-SUMMARY.md`
</output>
